import SafariDebugLogin from '../safari-debug';

export default function SafariDebugPage() {
  return <SafariDebugLogin />;
}

export const metadata = {
  title: 'Safari Debug Login - Brennholzkönig Admin',
  description: 'Safari-spezifische Debug-Seite für Login-Tests',
  robots: 'noindex, nofollow',
};